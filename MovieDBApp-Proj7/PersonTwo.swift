
import Foundation

//Holds the selected genre options for perosn two and has a boolean to determing if their turn is complete or not
struct PersonTwo {
    var completedSelections:Bool = false
    var selectedOptions:[[String:AnyObject]] = []
}
