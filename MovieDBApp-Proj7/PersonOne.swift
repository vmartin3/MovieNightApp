
import Foundation

//Holds the selected genre options for perosn one and has a boolean to determing if their turn is complete or not
struct PersonOne {
    var completedSelections:Bool = false
    var selectedOptions:[[String:AnyObject]] = []
}

